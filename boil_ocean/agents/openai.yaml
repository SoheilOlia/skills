interface:
  display_name: "Boil Ocean"
  short_description: "Ship finished work with tests and receipts"
  default_prompt: "Use $boil-ocean to implement this fully with tests, documentation, and honest receipts."
policy:
  allow_implicit_invocation: true
